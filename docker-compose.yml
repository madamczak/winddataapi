services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      # Path inside the container where SQLite .db files are mounted
      DATA_DIR: /data
    volumes:
      # Mount your local data_by_turbine folder into the container
      - ./data_by_turbine:/data:ro

